# 專案憲章：Rust + llama-cli + Gemma 4 E4B AI 每日新聞洞察

## 原則

1. **本機模型優先**：洞察產生必須透過本機 `llama-cli` 與 Gemma 4 E4B Q4_K_M GGUF，不依賴雲端 LLM API。
2. **來源透明**：摘要必須保留新聞來源、標題、URL 與發布時間。
3. **安全預設**：除非使用 `--send` 或排程腳本，預設不發送 ntfy。
4. **可檢查**：必須提供 `--dry-run`，讓沒有模型或網路的環境仍能產生示範輸出。
5. **不提交大型模型**：`.gguf` 與其他模型權重不得納入 Git。
6. **繁體中文輸出**：README、規格與 AI 洞察預設使用繁體中文。
