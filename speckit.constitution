# 專案憲章：Rust + llama-cli + Gemma 4 E4B GGUF Q4_K_M

## 原則

1. **本機優先**：範例必須能在沒有雲端 API 的情境下，透過本機 `llama-cli` 載入 GGUF 模型。
2. **小而清楚**：Rust 程式只負責參數組裝、環境變數支援、模型路徑驗證與行程啟動。
3. **不提交大型模型**：`.gguf` 與其他模型權重不得納入 Git。
4. **可檢查**：必須提供 `--dry-run` 與自動化測試，讓使用者在沒有模型檔時仍可驗證範例。
5. **繁體中文文件**：README 與規格文件以繁體中文描述使用方式。
